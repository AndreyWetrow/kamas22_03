const initialState = {
  friends: [
    { id: 1, name: "Oleg" },
    { id: 2, name: "Diego" },
    { id: 3, name: "Aragon" },
  ],
};

const sidebarReducer = (state = initialState, action) => {
  return state;
};

export default sidebarReducer;
